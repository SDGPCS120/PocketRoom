import 'package:flutter_test/flutter_test.dart';

void main() {
  test('blank test that always passes', () {
    // This test does nothing, it's supposed to pass(made to figure out the ci)
    expect(true, isTrue);
  });
}
