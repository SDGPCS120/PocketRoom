how to run the app:


run "flutter pub get"


run "flutter run -v" (the v is for verbose details, you can run it without the v but it'll look like nothing is happening)

also have an emulator set up, that makes your life easier 
