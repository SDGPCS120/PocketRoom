name: pocketroom
description: "pocketroom stuff"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  dio: ^5.4.0
  flutter_riverpod: ^2.5.1
  getwidget: ^7.0.0
  google_fonts: ^8.0.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
